jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Build Docker image
      run: docker build -t tcp_scanner .

    - name: Test compilation
      run: docker run --rm tcp_scanner --help || echo "Programme compilé, test basique passé"